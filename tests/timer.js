'use strict';

let test = require('tape');
let main = require('../app/components/timer');
 
test('prettyDate is an object', function (t) {
  t.plan(1);
  t.equal(Array.isArray(categoriesList), true, 'NOTE: be sure to use Array.isArray()');
  t.end();
});